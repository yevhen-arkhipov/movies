import styled from 'styled-components';
import { color, flexbox, layout, shadow, space } from 'styled-system';

const Box = styled.div`
  ${color}
  ${layout}
  ${flexbox}
  ${shadow}
  ${space}
`;

export default Box;
